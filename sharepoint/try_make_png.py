# just make png plot every few minutes, for website display
# drop_or_plot(state='testing' means no email sent

from try_alarm_email import drop_or_plot
drop_or_plot() # default = testing so no email sent